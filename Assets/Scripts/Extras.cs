using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Extras : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //if (Mathf.Approximately(rigidBody.velocity.y, 0) && Mathf.Approximately(rigidBody.velocity.x, 0))
        //{
        //    playerInMotion = false;
        //} else
        //{
        //    print(rigidBody.velocity);
        //    playerInMotion = true;
        //}
    }

        //public void OnCollisionStay2D(Collision2D collider)
        //{
        //xDist = this.transform.position.x - collider.transform.position.x;
        //yDist = this.transform.position.y - collider.transform.position.y;
        //if (xDist < 0.5f || yDist < 0.5f)
        //{
        //   xDist = 0.501f;
        //    yDist = 0.501f;
        //}
        //print ("Player collided with " + collider.gameObject.name);
        //playerInMotion = false;
        //}
}
